#include <stdio.h>
#include <stdlib.h>

int main()
{
  double width = 2.5;
  double height = 4.4;
  double area = width * height;
  double perimeter = (width + height) * 2;
  printf("The area is:%.2f", area);
  return 0;
}
